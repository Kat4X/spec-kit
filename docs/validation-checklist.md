# Validation checklist для Workflow Kit

Используй этот чеклист, чтобы проверить черновик на реальном или тестовом изменении.

## 1. Проверка структуры

- [ ] Change workspace создан в `ai/specs/YYYYMMDD_HHMM_change-name/`.
- [ ] Есть `spec.md`.
- [ ] Есть `plan.md`.
- [ ] Есть `scope.md`.
- [ ] Есть `tasks.json`.
- [ ] После `tasks` есть заготовка `verification.md`, после реализации она заполнена результатами.
- [ ] Опциональные файлы (`research.md`, `data-model.md`) созданы только если нужны.

## 2. Проверка `spec.md`

- [ ] Документ остаётся читаемым Markdown, без JSON/XML/YAML-обёртки всего spec.
- [ ] Понятно, зачем нужно изменение.
- [ ] Понятно, что должно стать правдой после изменения.
- [ ] Формат требований/кейсов подходит задаче: правила, сценарии, таблицы, flow, examples или edge cases.
- [ ] Проверяемые пункты действительно тестируемые/проверяемые.
- [ ] Есть критерии успеха.
- [ ] Есть out-of-scope.
- [ ] Неясности помечены `ТРЕБУЕТ УТОЧНЕНИЯ`.
- [ ] Spec отвечает на WHAT/WHY, а не HOW.
- [ ] В spec нет implementation details: файлов, классов, функций, библиотек, архитектурных решений и пошаговой реализации.

## 3. Проверка `plan.md`

- [ ] План отвечает на P0/обязательные требования или явно выбранный текущий scope.
- [ ] Есть список новых файлов.
- [ ] Есть список изменяемых файлов.
- [ ] Есть стратегия тестирования.
- [ ] Есть риски и митигации.
- [ ] Нет скрытого расширения scope.
- [ ] `scope.md` создан или обновлён как часть plan.

## 4. Проверка `scope.md`

- [ ] Есть `Freely editable`.
- [ ] Есть `Requires confirmation`.
- [ ] Есть `Forbidden`.
- [ ] Config/public API/migrations/shared files не попали в freely editable случайно.
- [ ] Агенту понятно, что делать с файлом, которого нет в списках.

## 5. Проверка `tasks.json`

- [ ] Файл валиден как JSON.
- [ ] Все задачи имеют `id` вида `T001`, `T002`, ... без пропусков.
- [ ] Все задачи имеют `title`, `details`, `status`, `priority`, `refs`, `dependsOn`, `files`, `checks`.
- [ ] `status` использует только допустимые значения: `pending`, `in_progress`, `done`, `blocked`, `skipped`.
- [ ] Каждая задача содержит явные `files` и команду проверки или проверяемый manual scenario.
- [ ] Задачи достаточно маленькие.
- [ ] P0 задачи идут раньше P1/P2.
- [ ] Есть финальная задача на проверки, обновление `verification.md` и проверку `scope.md`.
- [ ] `execution.order`, `dependsOn` и `parallel` задают понятный порядок выполнения.
- [ ] Параллельные задачи имеют `parallel: true`, а не текстовую метку.

## 6. Проверка implementation behavior

- [ ] Агент прочитал `spec.md`, `plan.md`, `scope.md`, `tasks.json` перед правками.
- [ ] В normal mode агент выбрал первую задачу со `status: "pending"` без незакрытых зависимостей или указанную задачу.
- [ ] В normal mode агент не выполнил несколько независимых задач без явного batch/YOLO-запроса.
- [ ] В batch/YOLO mode агент построил очередь только из dependency-ready задач без `confirmationRequired`.
- [ ] В batch/YOLO mode агент остановился на первом существенном blocker и не перепрыгнул через риск.
- [ ] Агент не менял files outside scope без подтверждения.
- [ ] Агент поставил `status: "done"` только после проверки, broader covering-check или явного объяснения.
- [ ] При blocker агент остановился, а не додумал рискованное решение.

## 7. Проверка `workflow-kit-list`

- [ ] Scanner запускается без зависимостей кроме Python 3.
- [ ] `READY` workspaces показывают progress, runnable tasks и команду запуска.
- [ ] `BLOCKED` workspaces показывают причину блокера.
- [ ] `DONE` скрыты по умолчанию и видны через `--all`.
- [ ] `--json` возвращает машинно-читаемый вывод.
- [ ] Scanner не меняет файлы.

## 8. Проверка `verification.md`

- [ ] Зафиксированы команды и результаты.
- [ ] Broader checks записаны как `covered`, если они заменили task-level checks.
- [ ] Зафиксированы ручные проверки или причина их отсутствия.
- [ ] Зафиксированы gaps/risks.
- [ ] `Ready` не используется при `pending`/`in_progress`/`blocked` обязательных задачах.
- [ ] Есть итоговое решение: Ready / Ready with warnings / Blocked / Needs user decision.

## 9. Красные флаги

Если видишь это — workflow надо править:

- [ ] Агент спрашивает процесс заново, хотя skill должен был объяснить.
- [ ] `scope.md` не помогает принять решение о правке файла.
- [ ] `tasks.json` превратился в крупные vague пункты.
- [ ] Verification выглядит как декоративная отписка.
- [ ] Маленькая правка требует слишком много документов.
- [ ] Большая правка всё равно теряет контекст.

## Решение после валидации

```markdown
# Validation result

Change: ...
Date: ...

## Worked well
- ...

## Failed / friction
- ...

## Changes to Workflow Kit
- ...

## Decision
- [ ] Keep as is
- [ ] Simplify
- [ ] Add missing artifact/rule
- [ ] Split skill
- [ ] Not viable
```
