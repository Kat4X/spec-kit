# Project Instructions

- Do not add, stage, or commit files/directories ignored by `.gitignore` unless the user explicitly asks for it.
- Do not use `git add -f` for ignored workflow artifacts without explicit user permission.
